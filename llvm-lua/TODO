
broken:
* LUA_HOOKTAILRET is broken for tail-calls from JIT functions.

optimizations:
* Remove jump from FORPREP to FORLOOP and copy loop test into FORPREP.  To save having to do (limit - pstep) in FORPREP and saves an extra call to FORLOOP.

